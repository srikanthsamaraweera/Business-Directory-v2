import "./register.css";
export const metadata = {
  title: "Registration",
  description: "Register an account to publish your business",
};
export default function Registration({ children }) {
  return <>{children}</>;
}
